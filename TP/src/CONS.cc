#include "CONS.h"

namespace Metricas
{

    CONS::CONS(unsigned WCOTACOES): Metrica(WCOTACOES)
    {
        this->_nomeMetrica = "CONS";
    }

    void CONS::calcularMetrica(Acao &acao, unsigned indiceMetrica){
        
    }
} // namespace Metrica