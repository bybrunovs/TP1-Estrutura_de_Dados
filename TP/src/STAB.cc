#include "STAB.h"

namespace Metricas
{

    STAB::STAB(unsigned WCOTACOES): Metrica(WCOTACOES)
    {
        this->_nomeMetrica = "STAB";
    }

    void STAB::calcularMetrica(Acao &acao, unsigned indiceMetrica){
        
    }
} // namespace Metrica